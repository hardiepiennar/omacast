#!/usr/bin/env python3
"""Development entry point; packaged installs should expose this on PATH."""

from pathlib import Path
import sys

root = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(root / "src"))

from omarchy_cast.cli import main  # noqa: E402

raise SystemExit(main())
