# Research metadata

This directory is historical phase-1 evidence, not package input. The network
example documents the client-DHCP shape that informed the production guard;
the guard generates its real unit at runtime from the discovered managed Wi-Fi
interface and a controller-issued session ID.
